<configuration>
	
	<!-- 
		
     Bot Main Configuration File
     ******************************

             | -=> www.goreclan.net <=- |
     ,__, .o0 ************************** 
     (oo)____
     (__)    )\
        ||**|| * 
		
		
	 -->
	 
	<settings name="log">
		<set name="path">log/Jbot.log</set>				<!-- The full path of the log file generated by the application. You can use both a relative path and an absolute path -->
		<set name="level">ALL</set>						<!-- The level of the log generated by the application: OFF, FATAL, ERROR, WARN, INFO, DEBUG, TRACE, ALL -->
		<set name="append">false</set>					<!-- Tell whether the application needs to generate a new log file on each startup -->
		<set name="console">true</set>					<!-- Tell whether log messages need to be printed in the System.out (JAVA console). Mostly used for development -->
	</settings>
</configuration>